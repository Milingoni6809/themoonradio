#!/bin/sh
# Gradle wrapper script (simplified — use Android Studio or install Gradle 8.3)
# For best results, open this project in Android Studio which will auto-download the wrapper.
exec gradle "$@"
